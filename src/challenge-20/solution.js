/**
 * @param {Array<{
 * 	type: string,
 * 	weightCapacity: number
 * }>} reindeerTypes
 * @param {Array<{
 * 	country: string,
 * 	weight: number
 * }>} gifts
 */
export function howManyReindeers(reindeerTypes, gifts) {
	return [];
}
